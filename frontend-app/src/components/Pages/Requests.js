import React, { useState } from "react";
import Header from "../Header/Header";

function Requests({modal,setModal,onOpen}) {
  
  return (
     <div>
      <Header/>
     </div>
    
  );
}

export default Requests;
